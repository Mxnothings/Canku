<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary_red</item>
        <item name="colorPrimaryDark">@color/color_primary_text</item>
        <item name="colorAccent">@color/color_primary_red</item>
        <item name="android:windowBackground">@color/color_primary_background</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:textColor">@color/color_primary_text</item>
    </style>

</resources>
